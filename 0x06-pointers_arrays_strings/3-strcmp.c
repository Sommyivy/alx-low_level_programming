#include "main.h"
#include <string.h>
/**
*_strcmp - a function that concatenates two strings
*@s1: copy to
*
*@s2: copy from
*
*Return: Always 0
*/
int _strcmp(char *s1, char *s2)
{
int ptr;
ptr = strcmp(s1, s2);
return (ptr);
}
